# GitSpace Web Terminal

This directory contains the GitSpace web terminal app served by the relay.

## Development

```bash
cd web
bun install
bun run dev
```

## Build

```bash
cd web
bun run build
```

The relay serves the built output from `web/dist`.
