# Mock Parity Build Plan (audit-derived)

Source: 6-agent audit of the agent-surfaces-app mock vs the real implementation (2026-07-05).
Execute top to bottom; verify with the checklist at the bottom (mock at :5199, real at :5173).

## 1. [p0-parity] Foundation: stage color tokens + STAGE_CAPS constant + shared deriveNoteLabel
**Files:** web/index.css, src/app/shared/workspace-detail/stage-caps.ts, src/components/note-label.ts, src/components/WorkspaceNotesModal.web.tsx

(a) Add --gs-stage-plan:#5b9bff, --gs-stage-code:#00ff66, --gs-stage-review:#ffcc00, --gs-stage-ship:#bc8cff to every theme block in web/index.css (mirror agent-surfaces-app/src/styles.css:22). (b) New src/app/shared/workspace-detail/stage-caps.ts exporting STAGE_CAPS keyed by WorkspacePhase: plan {note:'spec only · repo read-only', unlocks:['Goal / rubric / workflow authoring']}; code {note:'the only mode that edits the repo', unlocks:['Repo editable ✎','Workflows enabled ⟜','Agent edits + runs']}; review {note:'review the change', unlocks:['Diffs in file browser','Change Guide + rubric']}; ship {note:'post-merge ops', unlocks:['Crons & triggers live ◷','Roll up to project']} (mock.ts:1175-1181). (c) Extract deriveNoteLabel (first # heading else first non-empty line, 56-char cap) from WorkspaceNotesModal.web.tsx:23-28 into new src/components/note-label.ts. These three feed items 2, 6, and 7.

## 2. [p0-parity] Workspace sidebar header: workspace name + stage switcher dropdown + modecaps strip
**Files:** src/components/WorkspaceDetailPane.web.tsx, src/components/WorkspaceDetailPane.tsx, src/pages/WorkspaceDetailPage.web.tsx

Replace the 'Workspace Panel' label row (WorkspaceDetailPane.web.tsx:640-650) with the mock .sb-wh header (42px, px-[13px], border-b): mono workspace name (12.5px, ellipsis) + ml-auto stage button — transparent bg, 1px border, px-[7px] py-[2px], 10.5px uppercase tracking-[.05em], text AND border color = var(--gs-stage-{phase}) — label '{phase} ▾'. Click opens dropdown (absolute top-full right-0 mt-1, bg overlay, 1px --gs-border-active, min-w-[150px], z-30) listing all 4 stages: 7px stage-colored swatch + label + dim 10px STAGE_CAPS note + 'current' right-tag on the active one; selecting calls onSwitchStage (new prop on WorkspaceDetailPane.tsx, wired to workspaceBoardState.setPhase via WorkspaceDetailPage/app). Directly below, add the .modecaps strip: bg #070707, px-[13px] py-[7px], border-b, flex-wrap gap-[5px]; '{phase} mode' header (10px uppercase tracking-[.1em] dim) + one chip per STAGE_CAPS[phase].unlocks (10.5px text-muted, 1px --gs-border, px-1.5 py-px). Resolve phase from resolvePhase (useWorkspaceDetailModel.ts:49-61). Merges gaps: sidebar-workspace-header-stage-switcher, sidebar-mode-caps-strip, sidebar-stage-color-tokens, plan-stage-caps (display half).

## 3. [p0-parity] Workspace sidebar: regroup to mock order + Surfaces group + Workspace footer group
**Files:** src/components/WorkspaceDetailPane.web.tsx, src/components/WorkspaceDetailPane.tsx, src/app/shared/workspace-detail/useWorkspaceDetailModel.ts, src/pages/WorkspaceDetailPage.web.tsx

Reorder SidebarContent (WorkspaceDetailPane.web.tsx:216-414) to mock order: Agent → Terminals → Surfaces → Dashboards (item 4) → Services → Chain (item 5) → Workspace. Rename 'AI Agents'→'Agent'; keep real-only Agent Tasks under Agent; keep Replays/Notes/PM Links after Services. NEW 'Surfaces' group with 6 rows (Sidebar.tsx:82-88): ◇ Goal doc (rt = readiness passed/total from goal validation, else '—') → onOpenGoalDetail; ⟜ Workflow (rt 'live') → new onOpenWorkflow prop (stub to agent pane until item 22 lands); ⛓ Change Guide → onOpenReview; ☰ Review rubric → new onOpenRubric; ◷ Crons & triggers (rt 'ship') → new onOpenCrons; ⚑ Event logs → onOpenEvents. Delete the 'System' section and the footer Goal/Open Review duplicates. Convert the footer block (:396-414) into a 'Workspace' group with mock rows first: ◷ Change status, ⚙ Bundle config, ⌫ Delete (danger variant, rt 'danger', hover --gs-danger); keep real-only extras (Open GitHub PR, Auto Commit, Edit Process Config, Notes, pending-permissions) below the canonical three; update footerActions in useWorkspaceDetailModel.ts:354-367. Add the new onOpen* props to WorkspaceDetailPane.tsx and route them in WorkspaceDetailPage.web.tsx. Merges gaps: sidebar-section-order-grouping, sidebar-surfaces-group, sidebar-workspace-group-footer.

## 4. [p0-parity] Workspace sidebar: Dashboards group
**Files:** src/components/WorkspaceDetailPane.tsx, src/components/WorkspaceDetailPane.web.tsx, src/pages/WorkspaceDetailPage.web.tsx

Add dashboards?: {id,name,panelCount}[] + onOpenDashboard(id) + onCreateDashboard props to WorkspaceDetailPaneProps. Render a 'Dashboards' SidebarSection between Surfaces and Services: one ▦ row per dashboard with rt = panelCount, plus '＋ New dashboard' row. Wire in WorkspaceDetailPage.web.tsx from the same source DashboardPanel.web.tsx reads (dashboard artifacts on the mount); opening routes to the existing dashboard dock pane kind in app.web.tsx.

## 5. [p0-parity] Workspace sidebar: chain stack block
**Files:** src/components/WorkspaceDetailPane.tsx, src/components/WorkspaceDetailPane.web.tsx, src/pages/WorkspaceDetailPage.web.tsx

Add chainGoals?: KanbanGoalItem[] (type already in src/types/goals.ts:176-195) to WorkspaceDetailPaneProps; wire from the same source GoalDetailPanel gets chainGoals (app.web.tsx selectedGoalChainGoals pattern). When the workspace's goal has chainLength > 1, render after Services: header 'Chain · {chain title}' (.sb-grp but text-muted), then vertical stack per node: 9px rounded-full dot w/ 2px border on a 1px rail connector — shipped filled bg-[var(--gs-success)], active filled bg-[var(--gs-accent)] shadow-[0_0_8px_var(--gs-accent)], planned hollow border-[var(--gs-border-active)]; body = title (current node: text bright + font-medium), meta row = uppercase 10px phase label toned by status + mono 10.5px ready passed/total + 'here' chip (10.5px uppercase accent, 1px rgba(0,255,102,.3) border) on current. Non-current nodes with a workspace get hover bg + onClick → onSelectWorkspace(workspaceSelectionKey). Mock ref: Sidebar.tsx:21-44, styles.css:224-244.

## 6. [p0-parity] Notes: derived titles everywhere + new-note tab rekey + rail refresh + delete in NotePanel
**Files:** src/components/RightRail.web.tsx, src/pages/ProjectHomePage.web.tsx, src/app.web.tsx, src/components/NotePanel.web.tsx

(a) Use deriveNoteLabel (item 1) in RightRail.web.tsx:363 (currently `x.title ?? x.name ?? 'note'` → always 'note'), in ProjectHomePage.web.tsx:148-151 feed items, and for dock tab titles via onOpenNote (app.web.tsx:3008-3015). (b) In app.web.tsx note-pane branch (~2890-2907) pass onCreated to NotePanel: rewrite the matching dockExtraPanes entry to {kind:'note', noteId:created.id, title:deriveNoteLabel(created.body)} so the note:new-{nonce} tab rekeys and the dedupe guard (app.web.tsx:3012) works. (c) Add a notesVersion counter in app.web.tsx bumped on create/save/delete, passed into RightRail so ArtifactsMode refetches listWorkspaceNotes (currently fetched once, RightRail.web.tsx:356-372). (d) Add Delete to NotePanel header right cluster for existing notes (two-step confirm like the modal, WorkspaceNotesModal.web.tsx:168-177): call backend.removeWorkspaceNote, then onDeleted → close the tab (wire closeExtra from app.web.tsx) and bump notesVersion. Merges gaps: note-rail-titles-derived-from-body, new-note-tab-rekey-and-rail-refresh, rail-note-pane-refresh, note-delete-in-pane.

## 7. [p0-parity] Notes: clean cutover — delete WorkspaceNotesModal, reroute kebab actions to dock tabs
**Files:** src/components/WorkspaceNotesModal.web.tsx, src/app.web.tsx

Per project memory (prefer clean cutover): delete WorkspaceNotesModal.web.tsx entirely; in app.web.tsx remove notesModalState (176-183), openWorkspaceNotes/select/save/delete/create callbacks (~738-870), and the modal render (~2618-2632). Reroute workspace kebab onAddNote/onListNotes (app.web.tsx:2650-2655): if the workspace detail is open, push a {kind:'note'} dockExtraPane (noteId:null for add; first note for list); from the board, navigate into the workspace detail first, then open the note tab. Depends on item 6 (delete must exist in NotePanel first). Note deriveNoteLabel was already extracted in item 1 so nothing breaks on file deletion.

## 8. [p0-parity] Goal doc as workspace dock pane + chain strip + rail Goal group rows
**Files:** src/app.web.tsx, src/components/GoalDocPanel.web.tsx, src/components/GoalDetailPanel.web.tsx, src/components/RightRail.web.tsx

(a) New GoalDocPanel.web.tsx extracting DocTab + readiness from GoalDetailPanel.web.tsx:526-558, backed by resolveWorkspaceGoal (src/core/goal-chain.ts:276) via the backend; add {kind:'goal'} to DockExtraPane (app.web.tsx:133) + panel builder (~2843-2926), tab '◇ Goal'; include in default layout when the workspace has a goal. (b) Chain strip in both GoalDocPanel and GoalDetailPanel.web.tsx under the header (replacing static 'Goal {pos}/{len}' text at :264): horizontal track of one node per chainGoals entry — ⛓ icon toned shipped=--gs-success/active=--gs-accent/planned=--gs-text-dim, tabular index, title; current = bright text + inset 0 -2px accent underline; ‹ up / down › bordered nav buttons, disabled at ends; clicking a node swaps the displayed goal (lift selectedGoalKey setter, app.web.tsx:507; data already exists at :523-527). Mock ref: GoalDoc.tsx:23-33, mock.ts:748-752. (c) Synthesize a 'Goal' group at the top of RightRail ArtifactsMode from the workspace goal record: '◇ goal.md · doc' → open the goal dock pane; '⛓ chain · N goals · x of N' → goal pane; '☰ N requirements · rubric' → rubric pane (item 11); route goal.md file clicks to the goal pane instead of raw ArtifactPanel. Merges gaps: goal-doc-pane, chain-strip-in-doc, goal-artifact-rows, goal rows of rail-artifacts-pane-rows.

## 9. [p0-parity] Review as dock panes: 'review' (Change Guide) + 'rubric' pane kinds
**Files:** src/app.web.tsx, src/components/DockviewWorkspaceShell.web.tsx

Add 'review' and 'rubric' DockExtraPane kinds to the workspace dock (app.web.tsx panel builder ~2769-2930). '⛓ Change Guide' tab hosts the walkthrough (item 10; until then, mount the existing review surface's diff plumbing inside the pane via sendReviewRequest — do NOT swap the whole app to ReviewPage). '☰ Review rubric' tab hosts item 11's RubricPane. Wire the sidebar Surfaces rows from item 3 (Change Guide → open review pane; Review rubric → open rubric pane). Keep handleOpenReview (app.web.tsx:1995-2010) full-screen route working until Change Guide reaches parity, then retire the route in favor of the pane (keep GH import/push + thread/hunk state — real is higher fidelity, don't downgrade). Mock ref: Shell.tsx:23-27,63,116-117.

## 10. [p0-parity] Change Guide scrollytelling walkthrough pane
**Files:** src/components/ChangeGuide.web.tsx, src/types/review.ts, src/app.web.tsx

New ChangeGuide.web.tsx per mock stages/ReviewStage.tsx: 300px|1fr grid. Left rail: header 'Change Guide · the PR as a story', vertical numbered step timeline (9px dots — accent+glow active, green done, ✓ numbers, 1px connecting line), scroll-spy (container top +72px offset, bottom-snap), 'n / m phases reviewed' counter, active-step explain panel (uppercase KIND, what, accent 'WHY' prefix), foot with '☰ Review rubric' button (opens rubric pane) + 'Approve · n/m' primary (label drops count when all done). Right column: per-step bordered sections (green border + tinted header when done; scroll-margin-top), header = circled number/title/kind/'Mark complete' toggle, per-step file diffs rendered with the existing @pierre/diffs PatchDiff (RightRail.web.tsx:317 pattern) against real hunks, optional reviewer comment thread w/ tone (fail tone gets actions row — 'Send to agent' wire lands in item 24). Data: agent-authored walkthrough artifact WalkStep[]{n,kind,title,what,why,files[{path,hunks/lines}],comment?} persisted under the workspace artifacts mount (define convention, e.g. review/change-guide.json) or a new ReviewOperation in src/types/review.ts:139; steps reference real files/hunks; reuse ReviewThread data for comments. Mount as the 'review' pane kind from item 9.

## 11. [p0-parity] Review Rubric pane (two-column criteria/evidence/judgements)
**Files:** src/components/ReviewRubric.web.tsx, src/types/goals.ts, src/core/goal-validation.ts, src/app.web.tsx

New ReviewRubric.web.tsx rendering the workspace goal's GoalValidation as the mock 380px|1fr rubric (ReviewRubric.tsx): left = scroll-spy criterion index (verdict dot + rolled score) + active-criterion detail (verdict chip, 'required' tag, gate chip ◆human purple/✦llm blue/❯command green, contract text with `code` spans, 60px score bar toned ≥80 green/≥50 amber/else red, 'N judges · M evidence') + MakeJudgement form when human-gated & awaiting (pass/partial/fail picks, 0-100 slider, required note, 'Record judgement' → existing onRecordHumanReview handlers app.web.tsx:2706-2711) + foot 'N criteria · gated exit owned by human approval'; right = per-criterion sections (sticky header, 'awaiting your verdict' badge) with EVIDENCE cards (inline previews via item 12's EvidencePreview) and JUDGEMENTS rows. Type work: map RequirementStatus→verdict (missing→pending, review→partial, accepted→pass; add fail); rollup = any fail→fail, any partial|pending→partial, else pass; mean score. Keep GoalDetailPanel for authoring; this is the review-time judge surface, mounted as the 'rubric' pane kind (item 9) and opened from Change Guide foot + sidebar Surfaces. (Score/cites fields extended in item 21 — render gracefully when absent.) Merges gaps: review-rubric-pane, rubric-richness (layout half).

## 12. [p0-parity] Evidence: shared inline preview + 'ev:' evidence viewer dock pane
**Files:** src/components/EvidencePreview.web.tsx, src/components/GoalDetailPanel.web.tsx, src/components/ArtifactPanel.web.tsx, src/app.web.tsx

New EvidencePreview.web.tsx mirroring mock ArtifactPreview branches: stdout/body → black bordered mono pre (11px lh1.6); artifactPath + image/video mime → reuse ArtifactPreviewContent (ArtifactPanel.web.tsx:55); previewUrl → link row; plain path → 'file {path}' info row. Evidence type already has the data (src/types/goals.ts:28-46). Use it in GoalDetailPanel EvidenceSection (replace text-only EvidenceChip, :872-881) and in ReviewRubric (item 11) evidence cards with header: kind chip (command green/screenshot blue/video violet/review amber/note-file dim), mono name, meta, ref id right, captured(green, source command)/asserted(amber, manual) chip. Add {kind:'evidence'} DockExtraPane in app.web.tsx (id ev:<evidenceId>, tab '▸ {name}') resolving evidence from the goal record; make evidence rows in GoalDetailPanel/rubric/rail clickable to open it. Merges gaps: evidence-inline-previews-and-viewer-tab, rubric-richness (evidence half).

## 13. [p0-parity] RightRail repo mode: review stage awareness + diff-base select + change counts
**Files:** src/components/RightRail.web.tsx, src/types/review.ts, src/lib/remote-session/session-handler.ts

Add a stage/reviewing prop to RightRail (from workspace runtime phase). In review stage: Files header (RightRail.web.tsx:202-207) reads 'Diffs' + amber 'review' chip. Replace the static 'diff vs {baseBranch}' text with a diff-base <select> (main / merge-base minimum, per mock DIFF_BASES) driving the changed-files/diff requests — extend ReviewOperation get_changed_files/get_file_diff with a base param (src/types/review.ts) and handle serve-side. Add additions/deletions to ReviewChangedFile (src/types/review.ts:222-229) and render '+a −d' (10px dim tabular) in Changes rows (:245-254). Also add the header compTag ('backed by' ghost + '@pierre/trees' dim right-aligned). Merges gaps: rightrail-review-mode, diff-base + compTag parts of rail-section-header-chrome.

## 14. [p0-parity] RightRail artifacts mode: search + row meta + synthetic pane rows
**Files:** src/components/RightRail.web.tsx, src/app.web.tsx

(a) Add search input at top of ArtifactsMode (above group list, below the Favorites toggle — keep the toggle, real-only): bg-black, 1px --gs-border, 11px, px-[9px] py-[5px], focus:border-[var(--gs-accent)], placeholder 'search project artifacts…'; live-filters artifact entries + notes + (later) precedents/reports on path+kind+label. (b) In row() (:394-412) add ml-auto text-[10.5px] text-[var(--gs-text-dim)] meta span (derive: 'doc' for md, size/duration when cached, exit code for evidence logs); keep the hover fav ★ next to it; note rows get amber ● when dirty (full wiring item 20). (c) Prepend synthetic pane rows: '⚑ event log' → thread onOpenEvents into RightRail from app.web.tsx (~2976); '⟜ workflow · run' row when a workflow surface/agent run exists; Evidence rows from the goal's requirements opening ev: panes (item 12). Goal group rows landed in item 8. Merges gaps: rail-artifacts-search, rail-artifacts-row-meta, rail-artifacts-pane-rows (rest), evidence-group part of artifacts-rail-evidence-precedents-reports.

## 15. [p0-parity] Reports subsystem: report artifact format + ReportPanel pane + rail 'Reports · good + bad' group
**Files:** src/components/ReportPanel.web.tsx, src/components/RightRail.web.tsx, src/app.web.tsx, src/components/artifact-kinds.ts

Define report artifact convention: reports/*.report.json = ReportItem {kind: praise|good-pattern|frustration|workflow-quirk|gitspace-quirk, surface, note, attachments?:[{type: conversation|prompt|skill|tool|workflow-snapshot|goal-doc-snapshot, label, ref?}]}. In ArtifactsMode, parse report entries into a 'Reports · good + bad' group: rows = uppercase 10.5px kind chip with 1px border at 40% alpha of tone (praise --gs-info, good-pattern --gs-success, frustration --gs-danger, workflow-quirk --gs-warning, gitspace-quirk --gs-purple) + ellipsized surface, title attr = note; click → onOpenReport(path). New ReportPanel.web.tsx per mock ReportViewer.tsx: header [kind chip + mono surface], body max-w-[760px]: 'AGENT FEEDBACK' label + note in accent-left-border quote block, 'ATTACHMENTS — WHAT WAS REPORTED' rows (purple icon per type ❝⌜✦⛭⟜◇, uppercase type col, label, 'open ↗' resolving to existing openables). Add {kind:'report'} DockExtraPane (tab '⚑ Report') in app.web.tsx. Merges gaps: rail-artifacts-report-chips, report-to-gitspace.

## 16. [p0-parity] Rated precedents: 'Rated precedents · seed from these' rail group
**Files:** src/components/RightRail.web.tsx, src/session/backend.ts

Add a precedents source: simplest is extending the favorites store to 1-5 star ratings, or a listProjectPrecedents backend op over rated ship reports (reports/*.report.json with rating field — piggyback item 15's format). Render 'Rated precedents · seed from these' section in ArtifactsMode after Notes: rows = ⛓ + label + ml-auto text-[10px] text-[var(--gs-warning)] tracking-[1px] '★'.repeat(n)+'☆'.repeat(5-n), title attr = surface, click opens the goal pane (item 8); include in the search filter (item 14). Merges gaps: rail-artifacts-precedents, precedents part of artifacts-rail-evidence-precedents-reports.

## 17. [p0-parity] Project rail: WorkspaceCombo autocomplete replacing the native select
**Files:** src/components/WorkspaceCombo.web.tsx, src/pages/ProjectHomePage.web.tsx

New WorkspaceCombo.web.tsx per mock ProjectArtifactsRail.tsx:11-44: relative wrapper; absolute ⛓ span left-2 top-1.5 (11px dim, pointer-events-none); input font-mono text-[11px] bg-black border border-[var(--gs-border)] focus:border-[var(--gs-border-active)] pl-6 pr-5 py-[5px] w-full no-radius; absolute ▾ caret right-[7px] (10px dim). Closed value '{chain} · {ws}'; onFocus opens + clears query, placeholder 'find chain / workspace…', substring filter on '{chain} {ws}' lowercased, onBlur closes after 130ms. Dropdown: absolute inset-x-0 top-[29px] z-30 max-h-[240px] overflow-auto bg-[var(--gs-bg-overlay)] border border-[var(--gs-border-active)] shadow-[0_8px_24px_rgba(0,0,0,.6)]; chain group headers 10px uppercase tracking-[.1em] dim px-[9px] pt-[7px] pb-[3px]; options 11px mono px-[9px] py-1.5 hover bg-hover, selected bg-active, onMouseDown select; 'no matches' empty row. Replace the <select>/<optgroup> at ProjectHomePage.web.tsx:366-376; keep 'main (rolled up)' as first option under the project chain (real extension). Wrap in a .parts-loc header band: px-2.5 py-2 border-b border-[var(--gs-border)].

## 18. [p0-parity] Project rail: segmented tab bar + row anatomy + cross-workspace favorites
**Files:** src/pages/ProjectHomePage.web.tsx

(a) Tab bar (:377-380): wrap in flex border-b border-[var(--gs-border)]; buttons flex-1 py-[7px] text-[11px] text-muted, drop rounding; active = text-[var(--gs-text)] bg-[var(--gs-bg-elevated)] shadow-[inset_0_-2px_0_var(--gs-accent)]; favorites count as <span class ghost>{favs.size||''}</span> (hidden at 0). (b) railRow (:187-205): whole row clickable (keep star stopPropagation); gap-2 px-3 py-1 text-[11.5px], drop font-mono on the row, text-[var(--gs-text-muted)] hover:bg-[var(--gs-bg-hover)] hover:text-[var(--gs-text)]; star ALWAYS visible (remove opacity-0 group-hover) text-[12px] text-[var(--gs-text-ghost)] hover:text-muted, on → text-[var(--gs-warning)] (replace hardcoded #f0b429); keep lfs badge. Add optional sub?: string param rendering <span class='text-[10px] text-[var(--gs-text-dim)]'> · {sub}</span> after the name. (c) Favorites view (:170): when railView==='fav', aggregate favorites across ALL sources (fetch/cache listProjectArtifacts + listWorkspaceArtifacts per workspace), each row sub={workspace label}, and bind readArtifact/open to the owning source, not the global artifactSource; empty state 'No favorites yet — ★ an artifact to pin it across the project.' left-aligned px-3 py-[18px] text-[12px]. Merges gaps: rail-modes-tabbar, rail-row-anatomy, rail-favorites-cross-workspace.

## 19. [p1-subsystem] Workspace sidebar polish: litem primitive + terminal busy pulse + service port tags
**Files:** src/components/WorkspaceDetailPane.web.tsx, src/app/shared/workspace-detail/useWorkspaceDetailModel.ts

(a) Restyle SidebarItem/SidebarSection (:57-96, :45-55) to mock .litem: icon?: string glyph prop in w-[14px] centered dim span (accent when active, replaces 8px dot bullet), gap 9px px-[13px] py-[5px] text-[12px], NO border-radius; active = bg-active + shadow-[inset_2px_0_0_var(--gs-accent)]; right label 10.5px dim tabular-nums ml-auto; danger variant hover:text-[var(--gs-danger)]; section headers text-[10.5px] uppercase tracking-[.12em] text-dim, pt-[11px] px-[13px] pb-[5px]. Apply glyphs from mock: ▸ agent, ⌗ terminals, ◇/⟜/⛓/☰/◷/⚑ surfaces, ▦ dashboards, ● services, ⚙/⌫ workspace. (b) Terminals: busy/running sessions render right-aligned 7px animate-pulse bg-[var(--gs-accent)] rounded-full dot (:300-318). (c) serviceRows in useWorkspaceDetailModel.ts: expose port + failed state; dot toned ready/running --gs-success, stopped --gs-text-dim, failed --gs-danger; mono ':{port}' right tag; keep att/↗/stop buttons (real-only, higher fidelity). Merges gaps: sidebar-litem-row-styling, sidebar-terminal-busy-pulse, sidebar-service-port-tag.

## 20. [p1-subsystem] Notes polish: segmented Write|Preview, tab titles, dirty indicators, project-home note open, RPC kind preservation
**Files:** src/components/NotePanel.web.tsx, src/app.web.tsx, src/components/RightRail.web.tsx, src/pages/ProjectHomePage.web.tsx, src/lib/remote-session/session-handler.ts

(a) NotePanel.web.tsx:66-68: wrap Write/Preview in one inline-flex border border-[var(--gs-border)] group, square px-2.5 py-0.5 text-[11px] buttons, active bg-[var(--gs-bg-active)] text-[var(--gs-text)]; Save stays right. (b) app.web.tsx:2893: raise tab title truncation from 18 to ~28 chars with ellipsis. (c) Dirty: NotePanel onDirtyChange → app.web.tsx dockExtraPanes; append amber ● to the dock tab title; pass dirtyNoteIds into RightRail so matching note rows render <span class='text-[var(--gs-warning)]'>●</span>. (d) ProjectHomePage: extend FeedItem (:26-31) with {noteId, workspaceName}; make note rows clickable (currently disabled, :343-348) → render NotePanel in the center viewer slot (same as viewerPath/dashboardPath), titled via deriveNoteLabel. (e) session-handler.ts:1116: stop force-writing kind:'note' on updateWorkspaceNote — preserve existing kind/priority. Merges gaps: noteview-header-styling-parity, rail-dirty-indicator, project-home-note-open, note-rpc-kind-priority-flattened.

## 21. [p1-subsystem] Judgement model: type/score/cites on Review + render score bars and evidence cites
**Files:** src/types/goals.ts, src/core/goal-validation.ts, src/components/ReviewRubric.web.tsx, src/components/GoalDetailPanel.web.tsx

Extend Review (src/types/goals.ts:50-57) with type:'human'|'llm'|'command', score?:number (0-100), cites?:string[] (evidence ids). Writers in src/core/goal-validation.ts: runLlmJudgment (:692) prompt returns score + cited evidence ids; runJudgmentCommand (:645) cites the evidence it produced; recordHumanReview (:732) accepts optional score from the rubric slider. Migrate old records via migrateGoalRecord (:846). Render in RubricPane judgement rows (judge type icon ◆/✦/❯, 42x4 mini score bar toned ≥80 green/≥50 amber/else red, '↳ {evidence-id}' mono cites) and in GoalDetailPanel ReviewsSection (:903-911). Criterion rollup score = mean of scored judges. Merges gaps: judgement-model-scores-cites, remaining rubric-richness.

## 22. [p1-subsystem] Review-gated approval + 'Send to agent' from review threads
**Files:** src/components/ChangeGuide.web.tsx, src/app/shared/goal-validation/readiness.ts, src/components/ThreadPanel.web.tsx, src/app.web.tsx

(a) Gate Approve (and Push-to-GH submit): Change Guide 'Approve' stays 'Approve · n/m' and disabled until (1) all walkthrough phases marked complete AND (2) no required human-gated requirement still awaiting a human verdict (compute from readiness.ts + goal validation); surface goal readiness + human-gate status in the review pane header chip. Rubric foot already reads 'gated exit owned by human approval' (item 11). (b) 'Send to agent': on fail-tone Change Guide comments and review threads (ThreadPanel.web.tsx), add actions row ['✦ Send to agent → fix' primary, 'Comment', 'Dismiss']; Send composes thread target + body into a prompt injected into the workspace's active agent session (existing agent send path in app.web.tsx), marking the thread routed. Merges gaps: review-gated-approval, route-finding-to-agent.

## 23. [p1-subsystem] Block-composed goal doc: 5 new block types + exemplar starring + workflow tie-in card
**Files:** src/blocks/types/content.ts, src/blocks/render/content.web.tsx, src/core/goal-chain.ts, src/components/GoalDocPanel.web.tsx

(a) Add zod block types intent, boundaries, anti-shortcut, plan, evidence-shape to src/blocks/types/content.ts with web renderers in content.web.tsx matching mock blocks/index.tsx:246-294 (intent quote card w/ source+why; boundaries locked-tag/surface/rule rows; anti-shortcut ✕ shortcut/why rows; plan numbered steps w/ '↳ file:line' ref chips; evidence-shape table w/ command/test/screenshot kind chips); map mock 'mockup' → existing 'mini-app' block. (b) Store doc.blocks on GoalRecord alongside bodyMarkdown (src/core/goal-chain.ts:224-227) and render GoalDocPanel through the block pipeline when blocks exist, falling back to markdown. (c) Exemplar starring: absolute top-right ★ toggle per block (on = --gs-warning; starred block gets inset 2px warning left bar); persist doc.exemplarBlockIds on the goal record; '★ N exemplar' count in the pane header sub. (d) Footer .wf-tie card: full-width, 2px accent left border, ⟜ icon, 'This goal drives the review-gated workflow', 'open ↗' — links to the agent session / plan checklist (agentTodoPhases) until a workflow pane exists. Merges gaps: block-composed-doc, exemplar-starring, workflow-tiein.

## 24. [p1-subsystem] Rail chrome polish: RightRail segmented mode switch + group headers; project rail band structure
**Files:** src/components/RightRail.web.tsx, src/pages/ProjectHomePage.web.tsx

(a) RightRail.web.tsx:119-131: mode row → two flex-1 full-width uppercase 11px tracking-[.08em] py-[7px] segmented buttons, active = bg-[var(--gs-bg-elevated)] + shadow-[inset_0_-2px_0_var(--gs-accent)], container border-b; keep the ◨ collapse button trailing/overlaid. Group headers (:441,445) → text-[var(--gs-text-dim)] px-3 pt-2.5 pb-1 text-[10px] uppercase tracking-[.12em]; keep descriptive suffixes from items 15-16. (b) ProjectHomePage rail (:365): restructure to flex-col with no outer p-2 — band 1 combo header (done item 17), band 2 tab bar (done item 18), band 3 min-h-0 flex-1 overflow-y-auto py-1.5; group headers px-3 pt-[9px] pb-[3px] text-[10px] uppercase tracking-[.12em] dim aligning with px-3 rows; empty/error states inside band 3, left-aligned. Merges gaps: rail-mode-switch-chrome, remaining rail-section-header-chrome, rail-shell-sections.

## 25. [p1-subsystem] Cron/trigger subsystem: LastRunChip + trigger registry
**Files:** src/session/backend.ts, src/components/LastRunChip.web.tsx, src/pages/ProjectHomePage.web.tsx

Add SessionBackend op listProjectTriggers(project) → {id,name,kind:'cron'|'event'|'manual',when,status:'ok'|'pending'|'failed'|'idle',last,next,cost,writes:string[],history:('ok'|'fail'|'pending')[]} sourced from the crons/scheduled-agents store (implement in local backend + remote session-handler). New LastRunChip.web.tsx: compact chip '⟳ {last}' (transparent, 1px --gs-border, 10.5px, px-[5px], dim; hover text+border-active; stopPropagation) toggling popover (absolute right-0 top-6 w-[270px] bg-overlay border-active shadow, full-screen scrim z-40): header mono name + kind chip (cron blue/event violet/manual dim) + dim when; rows last-run status chip (ok green/pending amber/failed red/idle dim) + bold last + dim cost, next, mono writes list; 6x13px history sparkline dots; actions 'Run now' / 'Open trigger'. In ProjectHomePage railRow: when classifyArtifact(path)==='data' and a trigger's writes[] basename matches, render the chip between name and star. Gap: rail-cron-lastrun-chip.


# Verification checklist

- [ ] Item 1 — In real app (http://localhost:5173) devtools: getComputedStyle(document.documentElement).getPropertyValue('--gs-stage-code') returns #00ff66 (and plan/review/ship values) in every theme; deriveNoteLabel importable from src/components/note-label.ts (typecheck passes).
- [ ] Item 2 — Open a workspace in real vs mock (http://localhost:5199): sidebar top shows mono workspace name + colored stage button ('code ▾' in green when phase=code); clicking it opens a 4-row dropdown with swatch/label/dim note/'current'; selecting 'review' changes the button to amber and the phase persists; a '{stage} mode' chip strip appears under the header and the chips change per stage (plan shows 1 chip, code shows 3).
- [ ] Item 3 — Real sidebar group order reads Agent → Terminals → Surfaces → Dashboards → Services → (Chain) → Workspace, matching mock left sidebar; Surfaces has exactly the 6 rows with right-tags (Goal doc x/y, Workflow 'live', Crons 'ship'); no 'System' section; each row opens its pane; Workspace group starts with Change status / Bundle config / Delete and Delete shows rt 'danger' + red hover.
- [ ] Item 4 — Sidebar Dashboards group lists each dashboard with its panel count as the right tag; clicking opens the dashboard dock tab; '+ New dashboard' row present (compare mock Dashboards group).
- [ ] Item 5 — For a workspace in a multi-goal chain, sidebar shows 'Chain · {title}' with vertical dot+rail stack: green filled dots for shipped, glowing accent dot for active, hollow for planned, 'here' chip on the current node; clicking a sibling node with a workspace navigates to that workspace (mock: same block, and no chain block for single-goal workspaces).
- [ ] Item 6 — Rail Notes rows show derived titles (first heading), never the literal 'note'; creating a note via '+ New note', typing '# My title' and saving rekeys the tab to '✎ My title' and the rail list shows it without remount; Delete in the note pane header (two-step confirm) removes the note, closes the tab, and the rail updates.
- [ ] Item 7 — grep finds no WorkspaceNotesModal references; workspace kebab 'Add note'/'List notes' opens a note dock tab (never a modal), matching mock where notes are only tabs.
- [ ] Item 8 — Workspace dock shows a '◇ Goal' tab by default when a goal exists (mock default tabs agent/goal/workflow/review); the pane renders the goal doc with a chain strip of ⛓ nodes (status-colored, current underlined in accent); clicking a sibling swaps the doc body; ‹/› nav disabled at ends; RightRail artifacts mode shows a 'Goal' group ('goal.md · doc', 'chain · N goals', 'N requirements · rubric') and clicking goal.md opens the goal pane, not the raw file viewer.
- [ ] Item 9 — Sidebar 'Change Guide' and 'Review rubric' open dock tabs inside the workspace shell (tab strip shows '⛓ Change Guide' / '☰ Review rubric'); the app no longer swaps to a full-screen review route for these entries (mock: review/rubric are tabs next to agent/goal).
- [ ] Item 10 — Change Guide pane: left timeline lists steps with dots/numbers; scrolling the right column moves the active step (scroll-spy); 'Mark complete' turns the section border green, checks the timeline dot, and increments 'n / m phases reviewed'; foot shows 'Approve · n/m' until all steps done; per-step diffs render real file hunks (compare mock review tab at :5199).
- [ ] Item 11 — Rubric pane: two columns, left index rows with verdict dots + scores, scroll-spy sync; human-gated criterion shows purple 'your judgement' form (pass/partial/fail + slider + note, Record disabled until verdict+note); recording appends a judgement and clears 'awaiting your verdict'; gate chips render ◆ human purple / ✦ llm blue / ❯ command green (compare mock rubric tab).
- [ ] Item 12 — Evidence with stdout shows an inline mono pre; screenshot evidence shows the inline image; each evidence card has kind chip + captured(green)/asserted(amber) chip; clicking evidence opens a '▸ {name}' dock tab with the same preview (mock: EvidenceViewer tabs from Evidence rows).
- [ ] Item 13 — With workspace phase=review, RightRail repo mode header reads 'Diffs' with an amber 'review' chip; a 'diff vs' select offers main/merge-base and changing it updates the changed-file list; Changes rows show '+a −d' counts (mock: same header/select/counts in review stage).
- [ ] Item 14 — RightRail artifacts mode has a 'search project artifacts…' input that live-filters all groups; every artifact row has right-aligned dim meta; '⚑ event log' row opens the events pane and Evidence rows open ev: tabs (mock: Events/Evidence rows in artifactTree).
- [ ] Item 15 — Drop a reports/x.report.json into the artifacts mount: rail shows 'Reports · good + bad' with a toned uppercase kind chip (frustration=red border, praise=blue, etc.), hover tooltip = note; clicking opens the '⚑ Report' pane with quote block + typed attachment rows with 'open ↗' (mock: Reports group + ReportViewer).
- [ ] Item 16 — Rail shows 'Rated precedents · seed from these' with ⛓ rows and amber ★★★★☆ strings right-aligned; hover shows surface; click opens the goal pane; search filters them (mock: precedents group).
- [ ] Item 17 — Project home rail top is a mono input with ⛓ icon inside and ▾ caret showing 'chain · ws'; focusing clears and opens a styled dropdown with uppercase chain group headers; typing filters; 'no matches' on garbage; blur closes ~130ms later; 'main (rolled up)' still selectable (mock: WorkspaceCombo; real previously a native select).
- [ ] Item 18 — Project rail tabs are two full-width segmented buttons with accent underline on the active one and ghost count on Favorites (hidden at 0); rows: whole row clickable, star always visible turning --gs-warning when on; Favorites view shows pinned artifacts from ALL workspaces each with dim ' · {workspace}' suffix, and opening one from a non-selected workspace still loads its content.
- [ ] Item 19 — Sidebar rows show glyph icons (▸ ⌗ ◇ ▦ ● …) in a fixed dim column, active row gets 2px accent left inset bar + accent icon, rows are square-cornered; a busy terminal shows a pulsing 7px accent dot at the row's right edge; service rows show mono ':{port}' right tag with dot toned green/dim/red by state (pixel-compare against mock sidebar).
- [ ] Item 20 — Note pane Write|Preview is one joined bordered segment; dock tab titles fit ~28 chars; editing a note without saving shows amber ● on the tab and the rail row; project-home 'Reports & notes' note rows are clickable and open the note in the center pane; updating a todo-kind note via the UI preserves kind 'todo' in notes.json.
- [ ] Item 21 — LLM/command judgements persist and render with type icon, toned mini score bar, and '↳ evidence-id' cites in the rubric pane and GoalDetailPanel reviews list; criterion rollup score = mean of judge scores; old goal records still load (migration).
- [ ] Item 22 — Change Guide Approve stays disabled/labelled 'Approve · n/m' until all steps are marked AND every required human-gated requirement has a human verdict; a fail-tone thread shows '✦ Send to agent → fix' which injects the finding into the workspace agent session and marks the thread routed.
- [ ] Item 23 — A goal record with doc.blocks renders intent/boundaries/anti-shortcut/plan/evidence-shape blocks matching mock GoalDoc body; each block has a top-right ★ toggle that persists, adds a warning left bar, and updates '★ N exemplar' in the header; footer shows the accent-bordered workflow tie-in card with 'open ↗'.
- [ ] Item 24 — RightRail Repo/Artifacts switch is two full-width uppercase segments with accent bottom-inset on active (collapse button still reachable); project rail has bordered combo band / tab band / scrolling body, group headers aligned with row indent (pixel-compare both rails :5173 vs :5199).
- [ ] Item 25 — A data artifact whose name matches a trigger's writes[] shows the '⟳ {last}' chip; clicking opens the 270px popover with kind chip, last/next/writes, colored history sparkline, and Run now/Open trigger; clicking the chip does not open the artifact (stopPropagation); scrim click dismisses (mock: LastRunChip on data rows).